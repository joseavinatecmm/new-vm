
#include <stdio.h>
#include "util.h"
#define MAX 100
#define MAX 200

int global;
int global;

func suma(a,b) {
    int resultado;
    resultado = c;
    return resultado;
}

func suma(x) {
    return x;
}

func main() {
    int x;
    int x;

    x = y;
    suma(x);
    noExiste(x);

    {
        int z;
        z = x;
    }

    z = x;

    return noDeclarada;
}


