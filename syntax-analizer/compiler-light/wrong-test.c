#include <stdio.h>

#define MAX 100

int global

int suma(int a, int b) {
    int resultado;
    resultado = a + ;
    return resultado;
}

int main() {
    int x;
    x = 5;

    if (x > ) {
        x = x + 1;
    }

    return x;
}

