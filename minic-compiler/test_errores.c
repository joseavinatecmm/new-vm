#include <stdio.h>

#define MAX 100
#define MAX 200

int global
int global;

int suma(int a, int b) {
    int resultado;

    printf("Entrando a suma con error\n");

    resultado = c + a;

    printf("Resultado: %d\n", resultado);

    return resultado
}

int suma(int x) {
    printf("Función suma redeclarada\n");
    return x;
}

int main() {
    int x;
    int y;
    int noUsada;

    printf("Iniciando programa con errores\n");

    x = w;

    printf("Valor de x: %d\n", x);

    suma(x);

    noExiste(x);

    if (condicion) {
        int z;

        z = x;

        printf("Dentro del if\n");
        printf("Valor de z: %d\n", z);
    }

    z = x;

    printf("Fin del programa con errores\n");

    return noDeclarada
}
